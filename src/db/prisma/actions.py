# -*- coding: utf-8 -*-
# code generated by Prisma. DO NOT EDIT.
# pyright: reportUnusedImport=false
# fmt: off
from __future__ import annotations

# global imports for type checking
from builtins import bool as _bool
from builtins import int as _int
from builtins import float as _float
from builtins import str as _str
import sys
import decimal
import datetime
from typing import (
    TYPE_CHECKING,
    Optional,
    Iterable,
    Iterator,
    Sequence,
    Callable,
    ClassVar,
    NoReturn,
    TypeVar,
    Generic,
    Mapping,
    Tuple,
    Union,
    List,
    Dict,
    Type,
    Any,
    Set,
    overload,
    cast,
)
from typing_extensions import TypedDict, Literal


LiteralString = str
# -- template actions.py.jinja --
from typing import TypeVar
import warnings

from . import types, errors, bases
from ._compat import model_parse

if TYPE_CHECKING:
    from .client import Prisma
    from .bases import _PrismaModel


_PrismaModelT = TypeVar('_PrismaModelT', bound='_PrismaModel')


class ProfileActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.Profile]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await Profile.prisma().query_raw(
            'SELECT * FROM Profile WHERE userId = $1',
            'bbadfchfja',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.Profile
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await Profile.prisma().query_first(
            'SELECT * FROM Profile WHERE email = $1',
            'dhheabfhf',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.ProfileCreateInput,
        include: Optional[types.ProfileInclude] = None
    ) -> _PrismaModelT:
        """Create a new Profile record.

        Parameters
        ----------
        data
            Profile record data
        include
            Specifies which relations should be loaded on the returned Profile model

        Returns
        -------
        prisma.models.Profile
            The created Profile record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a Profile record from just the required fields
        profile = await Profile.prisma().create(
            data={
                # data to create a Profile record
                'userId': 'ggciceaie',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.ProfileCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple Profile records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of Profile record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await Profile.prisma().create_many(
            data=[
                {
                    # data to create a Profile record
                    'userId': 'bbehjachib',
                },
                {
                    # data to create a Profile record
                    'userId': 'cadfabfehe',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if self._client._active_provider == 'sqlite':
            raise errors.UnsupportedDatabaseError('sqlite', 'create_many()')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.ProfileWhereUniqueInput,
        include: Optional[types.ProfileInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single Profile record.

        Parameters
        ----------
        where
            Profile filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned Profile model

        Returns
        -------
        prisma.models.Profile
            The deleted Profile record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        profile = await Profile.prisma().delete(
            where={
                'userId': 'dgiiaaijj',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.ProfileWhereUniqueInput,
        include: Optional[types.ProfileInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique Profile record.

        Parameters
        ----------
        where
            Profile filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Profile model

        Returns
        -------
        prisma.models.Profile
            The found Profile record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        profile = await Profile.prisma().find_unique(
            where={
                'userId': 'bfaiacjjfc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.ProfileWhereUniqueInput,
        include: Optional[types.ProfileInclude] = None
    ) -> _PrismaModelT:
        """Find a unique Profile record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            Profile filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Profile model

        Returns
        -------
        prisma.models.Profile
            The found Profile record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        profile = await Profile.prisma().find_unique_or_raise(
            where={
                'userId': 'eigcfgbif',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ProfileWhereInput] = None,
        cursor: Optional[types.ProfileWhereUniqueInput] = None,
        include: Optional[types.ProfileInclude] = None,
        order: Optional[Union[types.ProfileOrderByInput, List[types.ProfileOrderByInput]]] = None,
        distinct: Optional[List[types.ProfileScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple Profile records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of Profile records returned
        skip
            Ignore the first N results
        where
            Profile filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Profile model
        order
            Order the returned Profile records by any field
        distinct
            Filter Profile records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.Profile]
            The list of all Profile records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 Profile records
        profiles = await Profile.prisma().find_many(take=10)

        # find the first 5 Profile records ordered by the createdAt field
        profiles = await Profile.prisma().find_many(
            take=5,
            order={
                'createdAt': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ProfileWhereInput] = None,
        cursor: Optional[types.ProfileWhereUniqueInput] = None,
        include: Optional[types.ProfileInclude] = None,
        order: Optional[Union[types.ProfileOrderByInput, List[types.ProfileOrderByInput]]] = None,
        distinct: Optional[List[types.ProfileScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single Profile record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Profile filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Profile model
        order
            Order the returned Profile records by any field
        distinct
            Filter Profile records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Profile
            The first Profile record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Profile record ordered by the userId field
        profile = await Profile.prisma().find_first(
            skip=1,
            order={
                'userId': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ProfileWhereInput] = None,
        cursor: Optional[types.ProfileWhereUniqueInput] = None,
        include: Optional[types.ProfileInclude] = None,
        order: Optional[Union[types.ProfileOrderByInput, List[types.ProfileOrderByInput]]] = None,
        distinct: Optional[List[types.ProfileScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single Profile record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Profile filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Profile model
        order
            Order the returned Profile records by any field
        distinct
            Filter Profile records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Profile
            The first Profile record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Profile record ordered by the email field
        profile = await Profile.prisma().find_first_or_raise(
            skip=1,
            order={
                'email': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.ProfileUpdateInput,
        where: types.ProfileWhereUniqueInput,
        include: Optional[types.ProfileInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single Profile record.

        Parameters
        ----------
        data
            Profile record data specifying what to update
        where
            Profile filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned Profile model

        Returns
        -------
        prisma.models.Profile
            The updated Profile record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        profile = await Profile.prisma().update(
            where={
                'userId': 'bagcfbhiig',
            },
            data={
                # data to update the Profile record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.ProfileWhereUniqueInput,
        data: types.ProfileUpsertInput,
        include: Optional[types.ProfileInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            Profile filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned Profile model

        Returns
        -------
        prisma.models.Profile
            The created or updated Profile record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        profile = await Profile.prisma().upsert(
            where={
                'userId': 'cghideieh',
            },
            data={
                'create': {
                    'userId': 'cghideieh',
                },
                'update': {
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.ProfileUpdateManyMutationInput,
        where: types.ProfileWhereInput,
    ) -> int:
        """Update multiple Profile records

        Parameters
        ----------
        data
            Profile data to update the selected Profile records to
        where
            Filter to select the Profile records to update

        Returns
        -------
        int
            The total number of Profile records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all Profile records
        total = await Profile.prisma().update_many(
            data={
                'createdAt': datetime.datetime.utcnow()
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ProfileWhereInput] = None,
        cursor: Optional[types.ProfileWhereUniqueInput] = None,
    ) -> int:
        """Count the number of Profile records present in the database

        Parameters
        ----------
        select
            Select the Profile fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Profile filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ProfileCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await Profile.prisma().count()

        # results: prisma.types.ProfileCountAggregateOutput
        results = await Profile.prisma().count(
            select={
                '_all': True,
                'userId': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.ProfileCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ProfileWhereInput] = None,
        cursor: Optional[types.ProfileWhereUniqueInput] = None,
    ) -> types.ProfileCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.ProfileCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ProfileWhereInput] = None,
        cursor: Optional[types.ProfileWhereUniqueInput] = None,
    ) -> Union[int, types.ProfileCountAggregateOutput]:
        """Count the number of Profile records present in the database

        Parameters
        ----------
        select
            Select the Profile fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Profile filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ProfileCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await Profile.prisma().count()

        # results: prisma.types.ProfileCountAggregateOutput
        results = await Profile.prisma().count(
            select={
                '_all': True,
                'email': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.ProfileCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.ProfileWhereInput] = None
    ) -> int:
        """Delete multiple Profile records.

        Parameters
        ----------
        where
            Optional Profile filter to find the records to be deleted

        Returns
        -------
        int
            The total number of Profile records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all Profile records
        total = await Profile.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.ProfileScalarFieldKeys'],
        *,
        where: Optional['types.ProfileWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.ProfileAvgAggregateInput'] = None,
        sum: Optional['types.ProfileSumAggregateInput'] = None,
        min: Optional['types.ProfileMinAggregateInput'] = None,
        max: Optional['types.ProfileMaxAggregateInput'] = None,
        having: Optional['types.ProfileScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.ProfileCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.ProfileScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.ProfileScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.ProfileGroupByOutput']:
        """Group Profile records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar Profile fields to group records by
        where
            Profile filter to select records
        take
            Limit the maximum number of Profile records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.ProfileGroupByOutput]
            A list of dictionaries representing the Profile record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group Profile records by createdAt values
        # and count how many records are in each group
        results = await Profile.prisma().group_by(
            ['createdAt'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class ReportActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.Report]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await Report.prisma().query_raw(
            'SELECT * FROM Report WHERE id = $1',
            'biabhbdai',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.Report
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await Report.prisma().query_first(
            'SELECT * FROM Report WHERE userId = $1',
            'idghgaicb',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.ReportCreateInput,
        include: Optional[types.ReportInclude] = None
    ) -> _PrismaModelT:
        """Create a new Report record.

        Parameters
        ----------
        data
            Report record data
        include
            Specifies which relations should be loaded on the returned Report model

        Returns
        -------
        prisma.models.Report
            The created Report record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a Report record from just the required fields
        report = await Report.prisma().create(
            data={
                # data to create a Report record
                'userId': 'fjfddhigg',
                'task': 'hjaecfifb',
                'extracted': Json({'cbbbjbfcii': True}),
                'missingFields': Json({'bbejhfidcb': True}),
                'warnings': Json({'bgeecijdgg': True}),
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.ReportCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple Report records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of Report record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await Report.prisma().create_many(
            data=[
                {
                    # data to create a Report record
                    'userId': 'bdiicjafbj',
                    'task': 'bgehebiafc',
                    'extracted': Json({'bghffegacj': True}),
                    'missingFields': Json({'bhghchehcc': True}),
                    'warnings': Json({'dcgchcbbf': True}),
                },
                {
                    # data to create a Report record
                    'userId': 'bdedcabahc',
                    'task': 'ghfhiafcb',
                    'extracted': Json({'heejgedji': True}),
                    'missingFields': Json({'bjgjgibgbf': True}),
                    'warnings': Json({'bbbgbhfjge': True}),
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if self._client._active_provider == 'sqlite':
            raise errors.UnsupportedDatabaseError('sqlite', 'create_many()')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.ReportWhereUniqueInput,
        include: Optional[types.ReportInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single Report record.

        Parameters
        ----------
        where
            Report filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned Report model

        Returns
        -------
        prisma.models.Report
            The deleted Report record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        report = await Report.prisma().delete(
            where={
                'id': 'igbehcbab',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.ReportWhereUniqueInput,
        include: Optional[types.ReportInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique Report record.

        Parameters
        ----------
        where
            Report filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Report model

        Returns
        -------
        prisma.models.Report
            The found Report record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        report = await Report.prisma().find_unique(
            where={
                'id': 'bdadaadhag',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.ReportWhereUniqueInput,
        include: Optional[types.ReportInclude] = None
    ) -> _PrismaModelT:
        """Find a unique Report record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            Report filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Report model

        Returns
        -------
        prisma.models.Report
            The found Report record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        report = await Report.prisma().find_unique_or_raise(
            where={
                'id': 'bgiggdidbf',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ReportWhereInput] = None,
        cursor: Optional[types.ReportWhereUniqueInput] = None,
        include: Optional[types.ReportInclude] = None,
        order: Optional[Union[types.ReportOrderByInput, List[types.ReportOrderByInput]]] = None,
        distinct: Optional[List[types.ReportScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple Report records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of Report records returned
        skip
            Ignore the first N results
        where
            Report filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Report model
        order
            Order the returned Report records by any field
        distinct
            Filter Report records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.Report]
            The list of all Report records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 Report records
        reports = await Report.prisma().find_many(take=10)

        # find the first 5 Report records ordered by the task field
        reports = await Report.prisma().find_many(
            take=5,
            order={
                'task': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ReportWhereInput] = None,
        cursor: Optional[types.ReportWhereUniqueInput] = None,
        include: Optional[types.ReportInclude] = None,
        order: Optional[Union[types.ReportOrderByInput, List[types.ReportOrderByInput]]] = None,
        distinct: Optional[List[types.ReportScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single Report record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Report filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Report model
        order
            Order the returned Report records by any field
        distinct
            Filter Report records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Report
            The first Report record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Report record ordered by the rawFilename field
        report = await Report.prisma().find_first(
            skip=1,
            order={
                'rawFilename': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ReportWhereInput] = None,
        cursor: Optional[types.ReportWhereUniqueInput] = None,
        include: Optional[types.ReportInclude] = None,
        order: Optional[Union[types.ReportOrderByInput, List[types.ReportOrderByInput]]] = None,
        distinct: Optional[List[types.ReportScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single Report record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Report filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Report model
        order
            Order the returned Report records by any field
        distinct
            Filter Report records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Report
            The first Report record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Report record ordered by the extracted field
        report = await Report.prisma().find_first_or_raise(
            skip=1,
            order={
                'extracted': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.ReportUpdateInput,
        where: types.ReportWhereUniqueInput,
        include: Optional[types.ReportInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single Report record.

        Parameters
        ----------
        data
            Report record data specifying what to update
        where
            Report filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned Report model

        Returns
        -------
        prisma.models.Report
            The updated Report record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        report = await Report.prisma().update(
            where={
                'id': 'caaaedabfc',
            },
            data={
                # data to update the Report record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.ReportWhereUniqueInput,
        data: types.ReportUpsertInput,
        include: Optional[types.ReportInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            Report filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned Report model

        Returns
        -------
        prisma.models.Report
            The created or updated Report record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        report = await Report.prisma().upsert(
            where={
                'id': 'bigibebcib',
            },
            data={
                'create': {
                    'id': 'bigibebcib',
                    'userId': 'bdedcabahc',
                    'task': 'ghfhiafcb',
                    'extracted': Json({'heejgedji': True}),
                    'missingFields': Json({'bjgjgibgbf': True}),
                    'warnings': Json({'bbbgbhfjge': True}),
                },
                'update': {
                    'userId': 'bdedcabahc',
                    'task': 'ghfhiafcb',
                    'extracted': Json({'heejgedji': True}),
                    'missingFields': Json({'bjgjgibgbf': True}),
                    'warnings': Json({'bbbgbhfjge': True}),
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.ReportUpdateManyMutationInput,
        where: types.ReportWhereInput,
    ) -> int:
        """Update multiple Report records

        Parameters
        ----------
        data
            Report data to update the selected Report records to
        where
            Filter to select the Report records to update

        Returns
        -------
        int
            The total number of Report records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all Report records
        total = await Report.prisma().update_many(
            data={
                'missingFields': Json({'bigaiehgcc': True})
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ReportWhereInput] = None,
        cursor: Optional[types.ReportWhereUniqueInput] = None,
    ) -> int:
        """Count the number of Report records present in the database

        Parameters
        ----------
        select
            Select the Report fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Report filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ReportCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await Report.prisma().count()

        # results: prisma.types.ReportCountAggregateOutput
        results = await Report.prisma().count(
            select={
                '_all': True,
                'warnings': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.ReportCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ReportWhereInput] = None,
        cursor: Optional[types.ReportWhereUniqueInput] = None,
    ) -> types.ReportCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.ReportCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ReportWhereInput] = None,
        cursor: Optional[types.ReportWhereUniqueInput] = None,
    ) -> Union[int, types.ReportCountAggregateOutput]:
        """Count the number of Report records present in the database

        Parameters
        ----------
        select
            Select the Report fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Report filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ReportCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await Report.prisma().count()

        # results: prisma.types.ReportCountAggregateOutput
        results = await Report.prisma().count(
            select={
                '_all': True,
                'rawOCR': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.ReportCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.ReportWhereInput] = None
    ) -> int:
        """Delete multiple Report records.

        Parameters
        ----------
        where
            Optional Report filter to find the records to be deleted

        Returns
        -------
        int
            The total number of Report records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all Report records
        total = await Report.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.ReportScalarFieldKeys'],
        *,
        where: Optional['types.ReportWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.ReportAvgAggregateInput'] = None,
        sum: Optional['types.ReportSumAggregateInput'] = None,
        min: Optional['types.ReportMinAggregateInput'] = None,
        max: Optional['types.ReportMaxAggregateInput'] = None,
        having: Optional['types.ReportScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.ReportCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.ReportScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.ReportScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.ReportGroupByOutput']:
        """Group Report records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar Report fields to group records by
        where
            Report filter to select records
        take
            Limit the maximum number of Report records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.ReportGroupByOutput]
            A list of dictionaries representing the Report record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group Report records by extractedMeta values
        # and count how many records are in each group
        results = await Report.prisma().group_by(
            ['extractedMeta'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class PredictionActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.Prediction]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await Prediction.prisma().query_raw(
            'SELECT * FROM Prediction WHERE id = $1',
            'beeifcbebf',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.Prediction
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await Prediction.prisma().query_first(
            'SELECT * FROM Prediction WHERE userId = $1',
            'bgcigfahea',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.PredictionCreateInput,
        include: Optional[types.PredictionInclude] = None
    ) -> _PrismaModelT:
        """Create a new Prediction record.

        Parameters
        ----------
        data
            Prediction record data
        include
            Specifies which relations should be loaded on the returned Prediction model

        Returns
        -------
        prisma.models.Prediction
            The created Prediction record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a Prediction record from just the required fields
        prediction = await Prediction.prisma().create(
            data={
                # data to create a Prediction record
                'userId': 'bcejgaggif',
                'task': 'idfjadbcc',
                'features': Json({'hgdhbjhhj': True}),
                'label': 429995104,
                'probability': 1775811865.89314,
                'healthScore': 995405759.210273,
                'topContributors': Json({'chbfcacbd': True}),
                'warnings': Json({'efggddide': True}),
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.PredictionCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple Prediction records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of Prediction record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await Prediction.prisma().create_many(
            data=[
                {
                    # data to create a Prediction record
                    'userId': 'caficfigfb',
                    'task': 'bfidgijfjc',
                    'features': Json({'ihieecagf': True}),
                    'label': 1675280054,
                    'probability': 1627576247.205480,
                    'healthScore': 60335757.68446,
                    'topContributors': Json({'bgcffadich': True}),
                    'warnings': Json({'fcbichhci': True}),
                },
                {
                    # data to create a Prediction record
                    'userId': 'bcggadccgf',
                    'task': 'jdcfdcgc',
                    'features': Json({'cafdaehjid': True}),
                    'label': 685333180,
                    'probability': 127474245.94892,
                    'healthScore': 1964990155.122889,
                    'topContributors': Json({'cffcachfd': True}),
                    'warnings': Json({'bccdfhdigc': True}),
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if self._client._active_provider == 'sqlite':
            raise errors.UnsupportedDatabaseError('sqlite', 'create_many()')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.PredictionWhereUniqueInput,
        include: Optional[types.PredictionInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single Prediction record.

        Parameters
        ----------
        where
            Prediction filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned Prediction model

        Returns
        -------
        prisma.models.Prediction
            The deleted Prediction record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        prediction = await Prediction.prisma().delete(
            where={
                'id': 'febcgjbfj',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.PredictionWhereUniqueInput,
        include: Optional[types.PredictionInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique Prediction record.

        Parameters
        ----------
        where
            Prediction filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Prediction model

        Returns
        -------
        prisma.models.Prediction
            The found Prediction record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        prediction = await Prediction.prisma().find_unique(
            where={
                'id': 'bageiegghg',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.PredictionWhereUniqueInput,
        include: Optional[types.PredictionInclude] = None
    ) -> _PrismaModelT:
        """Find a unique Prediction record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            Prediction filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Prediction model

        Returns
        -------
        prisma.models.Prediction
            The found Prediction record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        prediction = await Prediction.prisma().find_unique_or_raise(
            where={
                'id': 'faidicegb',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.PredictionWhereInput] = None,
        cursor: Optional[types.PredictionWhereUniqueInput] = None,
        include: Optional[types.PredictionInclude] = None,
        order: Optional[Union[types.PredictionOrderByInput, List[types.PredictionOrderByInput]]] = None,
        distinct: Optional[List[types.PredictionScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple Prediction records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of Prediction records returned
        skip
            Ignore the first N results
        where
            Prediction filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Prediction model
        order
            Order the returned Prediction records by any field
        distinct
            Filter Prediction records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.Prediction]
            The list of all Prediction records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 Prediction records
        predictions = await Prediction.prisma().find_many(take=10)

        # find the first 5 Prediction records ordered by the reportId field
        predictions = await Prediction.prisma().find_many(
            take=5,
            order={
                'reportId': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.PredictionWhereInput] = None,
        cursor: Optional[types.PredictionWhereUniqueInput] = None,
        include: Optional[types.PredictionInclude] = None,
        order: Optional[Union[types.PredictionOrderByInput, List[types.PredictionOrderByInput]]] = None,
        distinct: Optional[List[types.PredictionScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single Prediction record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Prediction filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Prediction model
        order
            Order the returned Prediction records by any field
        distinct
            Filter Prediction records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Prediction
            The first Prediction record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Prediction record ordered by the task field
        prediction = await Prediction.prisma().find_first(
            skip=1,
            order={
                'task': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.PredictionWhereInput] = None,
        cursor: Optional[types.PredictionWhereUniqueInput] = None,
        include: Optional[types.PredictionInclude] = None,
        order: Optional[Union[types.PredictionOrderByInput, List[types.PredictionOrderByInput]]] = None,
        distinct: Optional[List[types.PredictionScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single Prediction record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Prediction filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Prediction model
        order
            Order the returned Prediction records by any field
        distinct
            Filter Prediction records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Prediction
            The first Prediction record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Prediction record ordered by the features field
        prediction = await Prediction.prisma().find_first_or_raise(
            skip=1,
            order={
                'features': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.PredictionUpdateInput,
        where: types.PredictionWhereUniqueInput,
        include: Optional[types.PredictionInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single Prediction record.

        Parameters
        ----------
        data
            Prediction record data specifying what to update
        where
            Prediction filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned Prediction model

        Returns
        -------
        prisma.models.Prediction
            The updated Prediction record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        prediction = await Prediction.prisma().update(
            where={
                'id': 'bacecgfhbe',
            },
            data={
                # data to update the Prediction record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.PredictionWhereUniqueInput,
        data: types.PredictionUpsertInput,
        include: Optional[types.PredictionInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            Prediction filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned Prediction model

        Returns
        -------
        prisma.models.Prediction
            The created or updated Prediction record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        prediction = await Prediction.prisma().upsert(
            where={
                'id': 'ihcahiead',
            },
            data={
                'create': {
                    'id': 'ihcahiead',
                    'userId': 'bcggadccgf',
                    'task': 'jdcfdcgc',
                    'features': Json({'cafdaehjid': True}),
                    'label': 685333180,
                    'probability': 127474245.94892,
                    'healthScore': 1964990155.122889,
                    'topContributors': Json({'cffcachfd': True}),
                    'warnings': Json({'bccdfhdigc': True}),
                },
                'update': {
                    'userId': 'bcggadccgf',
                    'task': 'jdcfdcgc',
                    'features': Json({'cafdaehjid': True}),
                    'label': 685333180,
                    'probability': 127474245.94892,
                    'healthScore': 1964990155.122889,
                    'topContributors': Json({'cffcachfd': True}),
                    'warnings': Json({'bccdfhdigc': True}),
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.PredictionUpdateManyMutationInput,
        where: types.PredictionWhereInput,
    ) -> int:
        """Update multiple Prediction records

        Parameters
        ----------
        data
            Prediction data to update the selected Prediction records to
        where
            Filter to select the Prediction records to update

        Returns
        -------
        int
            The total number of Prediction records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all Prediction records
        total = await Prediction.prisma().update_many(
            data={
                'label': 1874748096
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.PredictionWhereInput] = None,
        cursor: Optional[types.PredictionWhereUniqueInput] = None,
    ) -> int:
        """Count the number of Prediction records present in the database

        Parameters
        ----------
        select
            Select the Prediction fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Prediction filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.PredictionCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await Prediction.prisma().count()

        # results: prisma.types.PredictionCountAggregateOutput
        results = await Prediction.prisma().count(
            select={
                '_all': True,
                'probability': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.PredictionCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.PredictionWhereInput] = None,
        cursor: Optional[types.PredictionWhereUniqueInput] = None,
    ) -> types.PredictionCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.PredictionCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.PredictionWhereInput] = None,
        cursor: Optional[types.PredictionWhereUniqueInput] = None,
    ) -> Union[int, types.PredictionCountAggregateOutput]:
        """Count the number of Prediction records present in the database

        Parameters
        ----------
        select
            Select the Prediction fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Prediction filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.PredictionCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await Prediction.prisma().count()

        # results: prisma.types.PredictionCountAggregateOutput
        results = await Prediction.prisma().count(
            select={
                '_all': True,
                'healthScore': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.PredictionCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.PredictionWhereInput] = None
    ) -> int:
        """Delete multiple Prediction records.

        Parameters
        ----------
        where
            Optional Prediction filter to find the records to be deleted

        Returns
        -------
        int
            The total number of Prediction records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all Prediction records
        total = await Prediction.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.PredictionScalarFieldKeys'],
        *,
        where: Optional['types.PredictionWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.PredictionAvgAggregateInput'] = None,
        sum: Optional['types.PredictionSumAggregateInput'] = None,
        min: Optional['types.PredictionMinAggregateInput'] = None,
        max: Optional['types.PredictionMaxAggregateInput'] = None,
        having: Optional['types.PredictionScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.PredictionCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.PredictionScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.PredictionScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.PredictionGroupByOutput']:
        """Group Prediction records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar Prediction fields to group records by
        where
            Prediction filter to select records
        take
            Limit the maximum number of Prediction records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.PredictionGroupByOutput]
            A list of dictionaries representing the Prediction record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group Prediction records by topContributors values
        # and count how many records are in each group
        results = await Prediction.prisma().group_by(
            ['topContributors'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class TriageNoteActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.TriageNote]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await TriageNote.prisma().query_raw(
            'SELECT * FROM TriageNote WHERE id = $1',
            'jbgijghgb',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.TriageNote
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await TriageNote.prisma().query_first(
            'SELECT * FROM TriageNote WHERE userId = $1',
            'hgjcghfbi',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.TriageNoteCreateInput,
        include: Optional[types.TriageNoteInclude] = None
    ) -> _PrismaModelT:
        """Create a new TriageNote record.

        Parameters
        ----------
        data
            TriageNote record data
        include
            Specifies which relations should be loaded on the returned TriageNote model

        Returns
        -------
        prisma.models.TriageNote
            The created TriageNote record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a TriageNote record from just the required fields
        triagenote = await TriageNote.prisma().create(
            data={
                # data to create a TriageNote record
                'userId': 'icadbcehj',
                'predictionId': 'jchciaee',
                'triageSummary': 'deeificjd',
                'followups': Json({'bbcbhebbda': True}),
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.TriageNoteCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple TriageNote records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of TriageNote record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await TriageNote.prisma().create_many(
            data=[
                {
                    # data to create a TriageNote record
                    'userId': 'bejfijgcfb',
                    'predictionId': 'caifcbgii',
                    'triageSummary': 'igaibbfgj',
                    'followups': Json({'bggajdcbbi': True}),
                },
                {
                    # data to create a TriageNote record
                    'userId': 'fcfhgbjed',
                    'predictionId': 'hdgcajhjg',
                    'triageSummary': 'ejdjahicb',
                    'followups': Json({'gdjgigfgc': True}),
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if self._client._active_provider == 'sqlite':
            raise errors.UnsupportedDatabaseError('sqlite', 'create_many()')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.TriageNoteWhereUniqueInput,
        include: Optional[types.TriageNoteInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single TriageNote record.

        Parameters
        ----------
        where
            TriageNote filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned TriageNote model

        Returns
        -------
        prisma.models.TriageNote
            The deleted TriageNote record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        triagenote = await TriageNote.prisma().delete(
            where={
                'id': 'gfeaahdeh',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.TriageNoteWhereUniqueInput,
        include: Optional[types.TriageNoteInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique TriageNote record.

        Parameters
        ----------
        where
            TriageNote filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned TriageNote model

        Returns
        -------
        prisma.models.TriageNote
            The found TriageNote record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        triagenote = await TriageNote.prisma().find_unique(
            where={
                'id': 'bjafcgbffc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.TriageNoteWhereUniqueInput,
        include: Optional[types.TriageNoteInclude] = None
    ) -> _PrismaModelT:
        """Find a unique TriageNote record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            TriageNote filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned TriageNote model

        Returns
        -------
        prisma.models.TriageNote
            The found TriageNote record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        triagenote = await TriageNote.prisma().find_unique_or_raise(
            where={
                'id': 'hihegjif',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.TriageNoteWhereInput] = None,
        cursor: Optional[types.TriageNoteWhereUniqueInput] = None,
        include: Optional[types.TriageNoteInclude] = None,
        order: Optional[Union[types.TriageNoteOrderByInput, List[types.TriageNoteOrderByInput]]] = None,
        distinct: Optional[List[types.TriageNoteScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple TriageNote records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of TriageNote records returned
        skip
            Ignore the first N results
        where
            TriageNote filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned TriageNote model
        order
            Order the returned TriageNote records by any field
        distinct
            Filter TriageNote records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.TriageNote]
            The list of all TriageNote records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 TriageNote records
        triagenotes = await TriageNote.prisma().find_many(take=10)

        # find the first 5 TriageNote records ordered by the predictionId field
        triagenotes = await TriageNote.prisma().find_many(
            take=5,
            order={
                'predictionId': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.TriageNoteWhereInput] = None,
        cursor: Optional[types.TriageNoteWhereUniqueInput] = None,
        include: Optional[types.TriageNoteInclude] = None,
        order: Optional[Union[types.TriageNoteOrderByInput, List[types.TriageNoteOrderByInput]]] = None,
        distinct: Optional[List[types.TriageNoteScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single TriageNote record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            TriageNote filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned TriageNote model
        order
            Order the returned TriageNote records by any field
        distinct
            Filter TriageNote records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.TriageNote
            The first TriageNote record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second TriageNote record ordered by the complaint field
        triagenote = await TriageNote.prisma().find_first(
            skip=1,
            order={
                'complaint': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.TriageNoteWhereInput] = None,
        cursor: Optional[types.TriageNoteWhereUniqueInput] = None,
        include: Optional[types.TriageNoteInclude] = None,
        order: Optional[Union[types.TriageNoteOrderByInput, List[types.TriageNoteOrderByInput]]] = None,
        distinct: Optional[List[types.TriageNoteScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single TriageNote record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            TriageNote filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned TriageNote model
        order
            Order the returned TriageNote records by any field
        distinct
            Filter TriageNote records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.TriageNote
            The first TriageNote record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second TriageNote record ordered by the triageSummary field
        triagenote = await TriageNote.prisma().find_first_or_raise(
            skip=1,
            order={
                'triageSummary': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.TriageNoteUpdateInput,
        where: types.TriageNoteWhereUniqueInput,
        include: Optional[types.TriageNoteInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single TriageNote record.

        Parameters
        ----------
        data
            TriageNote record data specifying what to update
        where
            TriageNote filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned TriageNote model

        Returns
        -------
        prisma.models.TriageNote
            The updated TriageNote record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        triagenote = await TriageNote.prisma().update(
            where={
                'id': 'bdjidcidac',
            },
            data={
                # data to update the TriageNote record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.TriageNoteWhereUniqueInput,
        data: types.TriageNoteUpsertInput,
        include: Optional[types.TriageNoteInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            TriageNote filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned TriageNote model

        Returns
        -------
        prisma.models.TriageNote
            The created or updated TriageNote record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        triagenote = await TriageNote.prisma().upsert(
            where={
                'id': 'ifgaaagff',
            },
            data={
                'create': {
                    'id': 'ifgaaagff',
                    'userId': 'fcfhgbjed',
                    'predictionId': 'hdgcajhjg',
                    'triageSummary': 'ejdjahicb',
                    'followups': Json({'gdjgigfgc': True}),
                },
                'update': {
                    'userId': 'fcfhgbjed',
                    'predictionId': 'hdgcajhjg',
                    'triageSummary': 'ejdjahicb',
                    'followups': Json({'gdjgigfgc': True}),
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.TriageNoteUpdateManyMutationInput,
        where: types.TriageNoteWhereInput,
    ) -> int:
        """Update multiple TriageNote records

        Parameters
        ----------
        data
            TriageNote data to update the selected TriageNote records to
        where
            Filter to select the TriageNote records to update

        Returns
        -------
        int
            The total number of TriageNote records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all TriageNote records
        total = await TriageNote.prisma().update_many(
            data={
                'followups': Json({'befcddgjce': True})
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.TriageNoteWhereInput] = None,
        cursor: Optional[types.TriageNoteWhereUniqueInput] = None,
    ) -> int:
        """Count the number of TriageNote records present in the database

        Parameters
        ----------
        select
            Select the TriageNote fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            TriageNote filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.TriageNoteCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await TriageNote.prisma().count()

        # results: prisma.types.TriageNoteCountAggregateOutput
        results = await TriageNote.prisma().count(
            select={
                '_all': True,
                'modelName': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.TriageNoteCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.TriageNoteWhereInput] = None,
        cursor: Optional[types.TriageNoteWhereUniqueInput] = None,
    ) -> types.TriageNoteCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.TriageNoteCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.TriageNoteWhereInput] = None,
        cursor: Optional[types.TriageNoteWhereUniqueInput] = None,
    ) -> Union[int, types.TriageNoteCountAggregateOutput]:
        """Count the number of TriageNote records present in the database

        Parameters
        ----------
        select
            Select the TriageNote fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            TriageNote filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.TriageNoteCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await TriageNote.prisma().count()

        # results: prisma.types.TriageNoteCountAggregateOutput
        results = await TriageNote.prisma().count(
            select={
                '_all': True,
                'createdAt': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.TriageNoteCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.TriageNoteWhereInput] = None
    ) -> int:
        """Delete multiple TriageNote records.

        Parameters
        ----------
        where
            Optional TriageNote filter to find the records to be deleted

        Returns
        -------
        int
            The total number of TriageNote records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all TriageNote records
        total = await TriageNote.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.TriageNoteScalarFieldKeys'],
        *,
        where: Optional['types.TriageNoteWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.TriageNoteAvgAggregateInput'] = None,
        sum: Optional['types.TriageNoteSumAggregateInput'] = None,
        min: Optional['types.TriageNoteMinAggregateInput'] = None,
        max: Optional['types.TriageNoteMaxAggregateInput'] = None,
        having: Optional['types.TriageNoteScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.TriageNoteCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.TriageNoteScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.TriageNoteScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.TriageNoteGroupByOutput']:
        """Group TriageNote records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar TriageNote fields to group records by
        where
            TriageNote filter to select records
        take
            Limit the maximum number of TriageNote records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.TriageNoteGroupByOutput]
            A list of dictionaries representing the TriageNote record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group TriageNote records by id values
        # and count how many records are in each group
        results = await TriageNote.prisma().group_by(
            ['id'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]



def _select_fields(root: str, select: Mapping[str, Any]) -> str:
    """Helper to build a GraphQL selection string

    This is a work around until field selection is added to the query builder.
    """

    return root + ' {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))


from . import models